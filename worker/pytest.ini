[pytest]
# The worker's modules are imported by bare name (from lifecycle import ...),
# matching how main.py imports them at runtime. Putting the worker directory on
# sys.path keeps the tests importing exactly what production imports.
pythonpath = .
testpaths = tests
