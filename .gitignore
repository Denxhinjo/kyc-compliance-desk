# Secrets — never commit. .env.example is the committed template.
.env
.env.local
.env.*.local

# Node / Next.js
node_modules/
.next/
.next-check/
out/
*.tsbuildinfo
next-env.d.ts

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Downloaded sanctions data. The synthetic fixture IS committed; real list
# data is fetched on demand and never checked in.
data/ofac_sdn.json
data/opensanctions.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
.vercel
.env*

# LaTeX build artefacts. The .tex and the .pdf are committed; nothing else.
docs/*.aux
docs/*.log
docs/*.out
docs/*.toc

# Personal working notes, kept out of the repo.
.project-notes.local.md
